import str,{lower} from './functions.js'

const myString = "ENSA FES Exam java"

console.log(lower(myString))
console.log(str(myString))

// we can do the '.mjs' extention if we dont have the package.json typing 